// Lenguaje de bajo nivel
// Nos comunicamos con una computadora (Ensamblador)

// Lenguaje de alto nivel
// Compilados
// C, C++ (CODEBLOCKS)

// Pseudocompilado
// Java

// Interpretrados
// JavaScript
// Python

//

//Javascript => ES6 (JS Moderno)
//(Chrome, IE, Safari, Firefox y Opera)

var nombre = "Gerardo";

// Todo está bien
let edad = 24;
edad = 25;

//Error
const pi = 3.1416;
const array = [];
const objeto = {};

// Funciones

//function suma(a, b) {
//    return a + b;
//}

// Arrow function
const suma = (a, b) => a + b;

const saludo = (nombre) => "Hola" + nombre;

console.log(suma(1, 5)); 
console.log(saludo("Braulio"));

// Decodificacion de carácteres Concatenacion
// const infoMascota = (nombre, raza, juguete) => "Mi mascota se llama " + nombre + ", es un" + raza + "y su juguete favorito es " + juguete;
const infoMascota = (nombre, raza, juguete) => "Mi mascota se llama " + nombre + ", es un" + raza + "y su juguete favorito es " + juguete;

